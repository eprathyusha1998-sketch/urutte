spring.application.name=urutte

# PostgreSQL settings
spring.datasource.url=jdbc:postgresql://localhost:5432/urutte_local
spring.datasource.username=urutte_user
spring.datasource.password=urutte123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server port
server.port=8080

# Google OAuth2 - LOCAL CONFIGURATION
# IMPORTANT: Make sure this redirect URI is added to Google Cloud Console
# Go to: https://console.cloud.google.com/apis/credentials
# Find OAuth client: 1051698753493-sn49e56i0qt9fqh47kd3dpcv11lk03v3.apps.googleusercontent.com
# Add redirect URI: http://localhost:8080/login/oauth2/code/google
spring.security.oauth2.client.registration.google.client-id=1051698753493-sn49e56i0qt9fqh47kd3dpcv11lk03v3.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/login/oauth2/code/google

# File upload configuration
spring.servlet.multipart.max-file-size=3MB
spring.servlet.multipart.max-request-size=3MB
spring.servlet.multipart.enabled=true

# File storage configuration
app.upload.dir=./uploads
app.upload.video-dir=./uploads/videos

# Frontend URL for OAuth redirect - LOCAL
app.frontend.url=http://localhost:3000
app.oauth2.redirect-uri=http://localhost:3000/login?token={token}
app.cors.allowed-origins=http://localhost:3000,http://localhost:8080

# AI Content Generation Configuration
app.ai.openai.api-key=${OPENAI_API_KEY:}
app.ai.openai.model=gpt-3.5-turbo
app.ai.openai.base-url=https://api.openai.com/v1

# AI Content Generation Settings (Production Mode - 4 Hour Intervals)
app.ai.content.generation.enabled=true
app.ai.content.generation.interval-hours=4
app.ai.content.generation.max-threads-per-topic=2
app.ai.content.generation.cleanup-days=30
app.ai.content.generation.free-credits-mode=true
