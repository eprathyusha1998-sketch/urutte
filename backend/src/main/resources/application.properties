spring.application.name=demo

# PostgreSQL settings
spring.datasource.url=jdbc:postgresql://postgres:5432/urutte
spring.datasource.username=urutte_user
spring.datasource.password=urutte_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server port
server.port=8080

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=1051698753493-qch8map8fnkeidvtiavlekofneh6bo86.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# File upload configuration
spring.servlet.multipart.max-file-size=3MB
spring.servlet.multipart.max-request-size=3MB
spring.servlet.multipart.enabled=true

# File storage configuration
app.upload.dir=./uploads
app.upload.video-dir=./uploads/videos

# Frontend URL for OAuth redirect
app.frontend.url=http://localhost
